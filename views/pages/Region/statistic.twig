{% extends 'pages/Region/baseRegion.twig' %}

{% block main %}
    {% include 'partials/vue-wrapper.twig' with {
        id: 'vue-genderlist',
        component: 'gender-list',
        props: {
            regionName: region.name,
            genderData: region.genderData,
            isWorkGroup: region.isWorkGroup
        }
    } %}
{% endblock %}
