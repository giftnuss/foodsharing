#!/bin/bash

export FS_ENV=${FS_ENV:-dev}

set -e

dir=$(dirname "$0")

. $dir/inc.sh

# directories used by the application

dirs="\
  images \
  images/basket \
  images/wallpost \
  images/picture \
  images/workgroup \
  data/attach \
  data/mailattach \
  data/mailattach/tmp \
  data/pass \
  data/visite \
  cache \
  cache/searchindex \
  cache/htmlpurifier-cache \
  tmp \
"

echo "Creating directories"

exec-in-container-asroot app \
  "mkdir -p $dirs && chmod 777 $dirs && chown -R www-data:www-data $dirs"

echo "Done"
